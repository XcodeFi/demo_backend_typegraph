export enum ProductStatusType {
    delete = -1,
    draft = 0,
    submitted = 1,
    reject = 2,
    publish = 3,
}

export enum StockStatus {
    out_stock = 0,
    in_stock = 1,
}

export enum FilterType {
    EQUALS = '=',

}